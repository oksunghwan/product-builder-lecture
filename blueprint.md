# 로또 번호 추천기

## 개요

이 애플리케이션은 사용자에게 로또 번호를 추천해주는 웹 애플리케이션입니다. 사용자는 "Generate 5 Sets" 버튼을 클릭하여 5개의 로또 번호 조합을 생성할 수 있습니다.

## 기능

-   **로또 번호 생성**: 1부터 45까지의 숫자 중 6개를 무작위로 선택하여 로또 번호 조합을 생성합니다.
-   **5개 조합 생성**: 한 번의 클릭으로 5개의 로또 번호 조합을 생성하여 화면에 표시합니다.
-   **테마 변경**: 다크 모드와 화이트 모드를 지원하여 사용자가 원하는 테마로 변경할 수 있습니다.

## 디자인 및 스타일

-   **레이아웃**: 중앙 정렬된 컨테이너에 콘텐츠를 표시하여 가독성을 높였습니다.
-   **색상**: 기본적으로 파란색 배경에 흰색 컨테이너를 사용하며, 다크 모드에서는 어두운 배경과 밝은 텍스트를 사용하여 눈의 피로를 줄입니다.
-   **폰트**: 'Courier New'와 같은 모노스페이스 폰트를 사용하여 복고적인 느낌을 줍니다.
-   **버튼**: 그림자 효과와 호버, 액티브 효과를 추가하여 사용자에게 상호작용의 피드백을 줍니다.
-   **아이콘**: `h1` 태그에 `🎲` 아이콘을 사용하여 재미를 더했습니다.

## 변경 사항

### 2026-02-01

-   **다크 모드, 화이트 모드 기능 추가**
    -   CSS 변수를 사용하여 다크/화이트 모드 테마를 구현했습니다.
    -   `style.css`에 다크 모드와 라이트 모드 스타일을 추가했습니다.
    -   `index.html`에 테마 변경 버튼을 추가했습니다.
    -   `main.js`에 테마 변경 버튼 클릭 시 `<html>` 태그의 `data-theme` 속성을 변경하는 로직을 추가했습니다.
-   **코드 리팩토링**
    -   `index.html`에 포함되어 있던 CSS와 JavaScript 코드를 각각 `style.css`와 `main.js` 파일로 분리했습니다.
    -   `index.html`에서 `style.css`와 `main.js`를 링크하도록 수정했습니다.
    -   `onclick` 속성을 제거하고 `addEventListener`를 사용하도록 `main.js`를 수정했습니다.
